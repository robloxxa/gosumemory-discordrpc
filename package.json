{
  "name": "gosumemory-discordrpc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "IF EXIST node_modules (node index) ELSE (npm install & node index)",
    "build": "IF EXIST node_modules (node build) ELSE (npm i --save-prod & node build)"
  },
  "keywords": ["discord rich presence", "osu!", "gosumemory", "osu", "discord activity"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord-rpc": "^4.0.1",
    "ws": "^8.0.0"
  },
  "devDependencies": {
    "caxa": "^2.1.0"
  }
}
